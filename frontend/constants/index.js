const networkMapping = require("./networkMapping.json")
const RestaurantManager = require("./RestaurantManager.json")
const ReservNFT = require("./ReservNFT.json")

module.exports = {
    networkMapping,
    RestaurantManager,
    ReservNFT,
}
